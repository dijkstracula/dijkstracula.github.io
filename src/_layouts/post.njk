---
layout: base.njk
---

<article>
  <header>
    <h1>{{ title }}</h1>
    <div class="post-meta">
      <time class="post-date" datetime="{{ date | isoDate }}">
        {{ date | readableDate }}
      </time>
      {% if tags %}
      <span> &middot; </span>
      {% for tag in tags %}
        {% if tag != "post" %}
          <span class="tag">#{{ tag }}</span>
        {% endif %}
      {% endfor %}
      {% endif %}
    </div>
  </header>

  {{ content | safe }}
</article>
